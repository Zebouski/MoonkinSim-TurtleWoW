image: node:latest

pages:
  cache:
    paths:
    - node_modules/

  script:
  - yarn install
  - yarn run build
  - yarn run docs
  - cp -R whitepaper dist/
  - mv dist public

  artifacts:
    paths:
    - public
  
  only:
  - master